[general]
ui = ttyui
accounts = puzzle
fsync = False
pythonfile = ~/.offlineimap.py

[Account puzzle]
localrepository = puzzle-local
remoterepository = puzzle-remote
status_backend = sqlite

[Repository puzzle-local]
type = Maildir
localfolders = ~/Mail/puzzle

[Repository puzzle-remote]
type = IMAP
remotehost = mail.puzzle.ch
remoteuser = bischof@puzzle.ch
remotepasseval = subprocess.check_output(["gpg", "-dq", ".passwd/puzzle.gpg"]).strip()
realdelete = no
folderfilter = lambda folder: folder in ['INBOX']

[mbnames]
enabled = yes
filename = ~/.mutt-mailboxes
header = "mailboxes "
peritem = "+%(accountname)s/%(foldername)s"
sep = " "
footer = "\n"
